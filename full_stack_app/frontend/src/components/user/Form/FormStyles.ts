import { Theme, createStyles } from "@material-ui/core";

export const FormStyles = (theme: Theme) => {
    return createStyles({
        formContainer: {
            marginTop: "10px"
        },
        formButton: {
            color: "#f1ee93"
        },
        paperContainer: {
            textAlign: "center",
        },
        buttonContainer: {
            
        }
    });
};