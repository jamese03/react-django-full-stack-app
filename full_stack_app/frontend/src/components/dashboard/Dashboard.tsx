import React from "react";
import { FormContainer } from "../user/Form/FormContainer";

export const Dashboard = () => {
    return (
        <div>
            <FormContainer />
        </div>
    );
};
