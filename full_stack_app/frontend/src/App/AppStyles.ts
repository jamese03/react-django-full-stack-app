import { Theme, createStyles } from "@material-ui/core";

export const AppStyles = (theme: Theme) => {
    return createStyles({
        contentContainer: {
            margin: "10px"
        }
    });
};